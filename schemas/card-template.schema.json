{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "card-template.schema.json",
  "title": "CardTemplateDefinition",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "version", "displayName", "content", "layouts", "visual"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Immutable published template version."
    },
    "displayName": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "default": "" },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "default": []
    },
    "content": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tone",
        "density",
        "headlineStyle",
        "headlineMaxChars",
        "itemTitleMaxChars",
        "itemBodyMaxChars"
      ],
      "properties": {
        "tone": { "type": "string", "minLength": 1 },
        "density": { "type": "string", "minLength": 1 },
        "headlineStyle": { "type": "string", "minLength": 1 },
        "headlineMaxChars": { "type": "integer", "minimum": 8 },
        "itemTitleMaxChars": { "type": "integer", "minimum": 4 },
        "itemBodyMaxChars": { "type": "integer", "minimum": 8 }
      }
    },
    "layouts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["allowed", "preferred", "maxItemsPerPage"],
      "properties": {
        "allowed": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": ["headline-list", "numbered-grid", "statement", "steps", "comparison"]
          }
        },
        "preferred": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "enum": ["headline-list", "numbered-grid", "statement", "steps", "comparison"]
          }
        },
        "maxItemsPerPage": { "type": "integer", "minimum": 0, "maximum": 6 }
      }
    },
    "visual": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tokens"],
      "properties": {
        "tokens": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        },
        "css": {
          "type": "string",
          "description": "Optional additive self-contained CSS. No @import or network url()."
        }
      }
    },
    "plannerGuidance": { "type": "string", "default": "" }
  }
}
